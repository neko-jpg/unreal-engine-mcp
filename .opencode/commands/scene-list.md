---
description: List scene database objects, optionally with filters
---

List scene objects using the provided arguments. Example:

```text
/scene-list --scene castle_crown_064013 --tag white_castle_crown
```

Command output:

!`python scripts/scenectl.py object list $ARGUMENTS`

Summarize the count and the most relevant rows.
