---
description: Run scripts/scenectl.py with the provided arguments
---

Run the scene database CLI exactly once with these arguments:

```text
$ARGUMENTS
```

Command output:

!`python scripts/scenectl.py $ARGUMENTS`

Summarize the result briefly. If the command failed, explain the failing check or stderr and the next concrete fix.
