---
description: Apply DB to Unreal sync operations for a scene
---

Apply DB to Unreal sync operations for the provided scene id. This command intentionally passes `--yes`.

Example:

```text
/scene-apply castle_crown_064013
```

Command output:

!`python scripts/scenectl.py apply --scene $1 --yes`

Summarize the apply summary and call out any failed or skipped operations.
