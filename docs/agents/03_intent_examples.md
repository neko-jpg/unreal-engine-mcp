# Intent 例文集

## Cave（洞窟）

### 基本生成
- `"create a cave"` / `"洞窟を作って"`
- `"generate dungeon"` / `"ダンジョンを生成して"`
- `"make a cavern"` / `"大洞窟を作って"`

### 雰囲気適用
- `"make cave creepy"` / `"洞窟を不気味にして"`
- `"dark cave atmosphere"` / `"暗い洞窟の雰囲気にして"`
- `"mysterious cave"` / `"神秘的な洞窟にして"`
- `"calm cave"` / `"静かな洞窟にして"`
- `"heroic cave"` / `"壮大な洞窟にして"`

### 検証・改善
- `"audit cave"` / `"洞窟を監査して"`
- `"validate cave"` / `"洞窟を検証して"`
- `"refine cave"` / `"洞窟を改善して"`
- `"fix cave issues"` / `"洞窟の問題を修正して"`

### 完全パイプライン
- `"full cave pipeline"` / `"洞窟の完全パイプラインを実行して"`
- `"complete cave generation"` / `"洞窟生成の全工程を実行して"`

## Architecture（建築）

### 建物
- `"build a house"` / `"家を建てて"`
- `"create mansion"` / `"豪邸を作って"`
- `"build castle"` / `"城を建てて"`
- `"construct tower"` / `"塔を建設して"`

### 構造物
- `"build bridge"` / `"橋を架けて"`
- `"create wall"` / `"壁を作って"`
- `"build pyramid"` / `"ピラミッドを建てて"`
- `"make maze"` / `"迷路を作って"`
- `"build aqueduct"` / `"水道橋を建てて"`

### 街・都市
- `"create town"` / `"街を作って"`
- `"build city"` / `"都市を建設して"`
- `"generate village"` / `"村を生成して"`

## Lighting（照明）

### 暗い照明
- `"dark lighting"` / `"暗い照明にして"`
- `"creepy lights"` / `"不気味な照明にして"`
- `"dim the lights"` / `"明かりを暗くして"`
- `"shadowy atmosphere"` / `"影のある雰囲気にして"`

### 明るい照明
- `"bright lights"` / `"明るい照明にして"`
- `"warm lighting"` / `"暖かい照明にして"`
- `"torch lights"` / `"松明の照明にして"`
- `"sunny atmosphere"` / `"晴れた雰囲気にして"`

### フォグ・雰囲気
- `"add fog"` / `"霧を追加して"`
- `"misty atmosphere"` / `"霧のある雰囲気にして"`
- `"volumetric fog"` / `"ボリュメトリックフォグを追加して"`
- `"sky atmosphere"` / `"空の雰囲気を設定して"`

## Material（マテリアル）

### 洞窟用
- `"wet stone material"` / `"湿った石のマテリアルにして"`
- `"cave material"` / `"洞窟用マテリアルを適用して"`
- `"rough stone"` / `"粗い石の質感にして"`
- `"mossy rock"` / `"苔むした岩にして"`

### 基本
- `"apply color"` / `"色を適用して"`
- `"red material"` / `"赤いマテリアルにして"`
- `"metallic surface"` / `"金属的な表面にして"`

## Landscape（地形）

### 作成・編集
- `"create landscape"` / `"地形を作って"`
- `"new terrain"` / `"新しい地形を生成して"`
- `"sculpt landscape"` / `"地形を彫刻して"`
- `"smooth terrain"` / `"地形を平滑化して"`

### マテリアル・植生
- `"apply landscape material"` / `"地形マテリアルを適用して"`
- `"add grass"` / `"草を追加して"`
- `"paint foliage"` / `"植生をペイントして"`
- `"procedural foliage"` / `"手続き的に植生を生成して"`
- `"scatter trees"` / `"木を散りばめて"`

## Foliage（植生）

### ペイント
- `"paint grass"` / `"草をペイントして"`
- `"paint trees"` / `"木をペイントして"`
- `"erase foliage"` / `"植生を消去して"`

### 生成
- `"spawn procedural foliage"` / `"手続き的に植生を生成して"`
- `"create foliage type"` / `"フォリエージタイプを作成して"`
- `"register mesh foliage"` / `"メッシュフォリエージを登録して"`

## Validation（検証）

### 完全検証
- `"run full validation"` / `"完全検証を実行して"`
- `"validate scene"` / `"シーンを検証して"`
- `"check all"` / `"全てチェックして"`

### 個別検証
- `"validate collision"` / `"衝突を検証して"`
- `"check navigation"` / `"ナビゲーションを確認して"`
- `"performance test"` / `"パフォーマンステストを実行して"`
- `"screenshot test"` / `"スクリーンショットテストを実行して"`

## NPC / AI

### スポーン・行動
- `"spawn NPC"` / `"NPCをスポーンして"`
- `"create enemy"` / `"敵を作成して"`
- `"set AI behavior"` / `"AI行動を設定して"`
- `"create patrol route"` / `"巡回ルートを作成して"`
- `"add navmesh"` / `"ナビメッシュを追加して"`

## Cinematic（シネマティック）

### カメラ・シーケンス
- `"spawn camera"` / `"カメラをスポーンして"`
- `"create sequence"` / `"シーケンスを作成して"`
- `"add camera track"` / `"カメラトラックを追加して"`
- `"render shot"` / `"ショットをレンダリングして"`

## UI

### ウィジェット
- `"create widget"` / `"ウィジェットを作成して"`
- `"add button"` / `"ボタンを追加して"`
- `"show HUD"` / `"HUDを表示して"`
- `"create menu"` / `"メニューを作成して"`

## Physics（物理）

### アクター・拘束
- `"spawn physics actor"` / `"物理アクターをスポーンして"`
- `"add constraint"` / `"拘束を追加して"`
- `"enable ragdoll"` / `"ラグドールを有効化して"`
- `"spawn destructible"` / `"破壊可能オブジェクトをスポーンして"`

## Audio（音声）

### サウンド
- `"play sound"` / `"音を再生して"`
- `"ambient audio"` / `"アンビエント音声を設定して"`
- `"footstep sounds"` / `"足音を設定して"`
- `"background music"` / `"BGMを設定して"`

## VFX（視覚効果）

### パーティクル
- `"add particles"` / `"パーティクルを追加して"`
- `"smoke effect"` / `"煙エフェクトを追加して"`
- `"dust particles"` / `"ほこりパーティクルを追加して"`
- `"fire effect"` / `"炎エフェクトを追加して"`

## Animation（アニメーション）

### ブループリント・モーション
- `"create anim blueprint"` / `"アニメーションBPを作成して"`
- `"add state machine"` / `"ステートマシンを追加して"`
- `"create blend space"` / `"ブレンドスペースを作成して"`

## Gameplay（ゲームプレイ）

### ゲームモード・キャラクター
- `"create gamemode"` / `"ゲームモードを作成して"`
- `"create character"` / `"キャラクターを作成して"`
- `"grant ability"` / `"アビリティを付与して"`
- `"save game"` / `"ゲームをセーブして"`

## Networking（ネットワーク）

### セッション
- `"create session"` / `"セッションを作成して"`
- `"find multiplayer games"` / `"マルチプレイヤーゲームを検索して"`
- `"join session"` / `"セッションに参加して"`
- `"start server"` / `"サーバーを起動して"`

## Import / Export（インポート/エクスポート）

### アセット
- `"import FBX"` / `"FBXをインポートして"`
- `"import GLTF"` / `"GLTFをインポートして"`
- `"export mesh"` / `"メッシュをエクスポートして"`
- `"import texture"` / `"テクスチャをインポートして"`

## Asset Management（アセット管理）

### フォルダ・操作
- `"create folder"` / `"フォルダを作成して"`
- `"move asset"` / `"アセットを移動して"`
- `"delete asset"` / `"アセットを削除して"`
- `"fix redirectors"` / `"リダイレクタを修正して"`

## Level Management（レベル管理）

### レベル
- `"create level"` / `"レベルを作成して"`
- `"load map"` / `"マップをロードして"`
- `"save level"` / `"レベルを保存して"`
- `"add sublevel"` / `"サブレベルを追加して"`

## Project Editor（プロジェクト/エディタ）

### 設定・ビルド
- `"project settings"` / `"プロジェクト設定を変更して"`
- `"build project"` / `"プロジェクトをビルドして"`
- `"package game"` / `"ゲームをパッケージングして"`
- `"editor settings"` / `"エディタ設定を変更して"`

## 複合意図（マルチドメイン）

### Cave + Lighting
- `"create creepy cave with dark lighting"` / `"暗い照明付きの不気味な洞窟を作って"`
- `"generate cave and add fog"` / `"洞窟を生成して霧を追加して"`

### Architecture + Landscape
- `"build castle on flat terrain"` / `"平坦な地形に城を建てて"`
- `"create town with landscape"` / `"地形付きの街を作って"`

### Landscape + Foliage
- `"create terrain with trees"` / `"木がある地形を作って"`
- `"generate landscape and paint grass"` / `"地形を生成して草をペイントして"`

### Validation + その他
- `"validate scene after changes"` / `"変更後にシーンを検証して"`
- `"check performance after building"` / `"建設後にパフォーマンスを確認して"`
